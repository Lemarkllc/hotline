/**
 * «Получить VPN» (боковое меню бота-сотрудника, vpnService.ts) — набор inbound'ов
 * (сервера/протоколы в 3X-UI), которые получает новый профиль. Скопирован с
 * реального профиля рядового сотрудника (a.arhipova) — только DE/FR, без VIP/IT
 * дополнений (16 и 13 inbound'ов у тех групп соответственно), решение пользователя
 * 2026-09-22. ID захардкожены осознанно: те же ID, что уже назначены остальным
 * сотрудникам, у них нет причин меняться — если набор серверов изменится, правки
 * только здесь.
 */
export const VPN_STANDARD_INBOUND_IDS = [1, 881, 882, 883, 884, 885, 886, 906, 912] as const;

/** «Два устройства» (решение пользователя 2026-09-22, подтверждено 2026-09-23) —
 * в терминах 3X-UI это HWID Limit (число уникальных hardware ID устройств,
 * когда-либо использовавших профиль). IP Limit сознательно НЕ используется
 * (решение пользователя 2026-09-23: нужен только лимит по числу устройств, не по
 * одновременным IP/подключениям) — ставим 0 (см. vpnPanelService.createClient).
 * Клиент с HWID Limit > 0 отдаёт подписку только запросам с заголовком X-HWID —
 * это штатное поведение панели (device-fingerprint header, который реальные
 * приложения Happ/Incy/v2rayNG отправляют сами), НЕ баг: голый curl/браузер без
 * этого заголовка получит 404 на /sub/<subId>, что подтверждено вживую 2026-09-23
 * и не должно приниматься за неисправность профиля. Лимита скорости/трафика
 * панель не поддерживает на уровне клиента — проверено по схеме БД 3X-UI, нет
 * такого поля ни у clients, ни у inbounds/nodes. */
export const VPN_PROFILE_HWID_LIMIT = 2;
