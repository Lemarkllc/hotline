/**
 * «Получить VPN» (боковое меню бота-сотрудника, vpnService.ts) — набор inbound'ов
 * (сервера/протоколы в 3X-UI), которые получает новый профиль. Скопирован с
 * реального профиля рядового сотрудника (a.arhipova) — только DE/FR, без VIP/IT
 * дополнений (16 и 13 inbound'ов у тех групп соответственно), решение пользователя
 * 2026-09-22. ID захардкожены осознанно: те же ID, что уже назначены остальным
 * сотрудникам, у них нет причин меняться — если набор серверов изменится, правки
 * только здесь.
 */
export const VPN_STANDARD_INBOUND_IDS = [1, 881, 882, 883, 884, 885, 886, 906, 912] as const;

/** «Два устройства» (решение пользователя 2026-09-22) — в терминах 3X-UI это HWID
 * Limit (число уникальных hardware ID устройств, когда-либо использовавших профиль),
 * не IP Limit (одновременные подключения/IP — другое понятие, реальная ошибка
 * найдена пользователем вживую 2026-09-22: стоял только IP Limit=2, HWID Limit
 * оставался безлимитным). Ставим оба в 2 — так проверка бьёт и по числу устройств,
 * и по числу одновременных подключений. Лимита скорости/трафика панель не
 * поддерживает на уровне клиента (проверено по схеме БД 3X-UI — нет такого поля
 * ни у clients, ни у inbounds/nodes). */
export const VPN_PROFILE_IP_LIMIT = 2;
export const VPN_PROFILE_HWID_LIMIT = 2;
